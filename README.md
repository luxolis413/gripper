# Download the sdk from https://www.jodell-robotics.com/dowmload-center
# Extract the files and go to the directory and then Install the sdk with pip install pip install joddtell-0.1.2-py3-NONE-ANY. WHL
# Allow permission to use the RS-485 to USB communication by running sudo chmod 666 /dev/ttyUSB0 
